import styled from "styled-components";

export const FormFileName = styled.p`
  font-weight: bold;
  padding: 0;
  margin: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;
